export SEQERA_ORGANIZATION_NAME=community          ## [CHANGE ME] Seqera Platform Organization name
export SEQERA_WORKSPACE_NAME=showcase              ## [CHANGE ME] Seqera Platform Workspace name
export COMPUTE_NAME_PREFIX                         ## [CHANGE ME] A prefix for your compute environmet names. These can be informative of your compute account or region being used (e.g. "biorad-virginia" or "benchmarks-virginia")
export TIME=`date +"%Y%m%d-%H%M%S"`                ## Bash timestamp used to dynamically name pipeline runs and results directories